import 'package:flutter/material.dart';

class MedicalRemindersScreen extends StatelessWidget {
  const MedicalRemindersScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Medical Reminders')),
      body: Center(child: Text('Medical reminders placeholder')),
    );
  }
}
